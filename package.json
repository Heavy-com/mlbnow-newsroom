{
  "name": "heavy-on-mlb-newsroom",
  "version": "2.0.0",
  "description": "Heavy on MLB live newsroom dashboard",
  "scripts": {
    "start": "node api/news.js",
    "test": "node test/signal.test.js",
    "check": "for f in api/*.js lib/*.js; do node --check \"$f\"; done"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
